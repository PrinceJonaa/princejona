import FractalVision from "../../components/FractalVision";

/**
 * Fractal Vision Page
 * Animated, interactive diagram connecting self, projects, humanity.
 * Placeholder for interactive SVG/canvas animation.
 */
export default function FractalVisionPage() {
  return (
    <main style={{ minHeight: "100vh", display: "flex", alignItems: "center", justifyContent: "center" }}>
      <FractalVision />
    </main>
  );
}
